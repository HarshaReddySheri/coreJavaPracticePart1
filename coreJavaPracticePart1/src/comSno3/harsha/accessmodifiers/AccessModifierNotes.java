package comSno3.harsha.accessmodifiers;

public class AccessModifierNotes {
	/*
	 * public is accessible every where
	 * 			--> within class
	 * 			-->  "	   SamePackage
	 * 			-->  "	   differentClass
	 * 			-->  "     differentPackage
	 * 			-->  "     OutSide Project
	 * 
	 * private only within the class
	 * 			--> no where else
	 * default only package private
	 * 			--> within the package you can access anyWhere & Subclass
	 * protected only within package in every class
	 * 			-->outside the package only subclass can access
	 * 
	 *  
	 * OR
	 * 
	 * Class private -> private
	 * package private -> default
	 * package private with extra future		--> protected 
	 *(outside the package only subclass can access) 
	 * No private -> public
	 *
	 */
}
